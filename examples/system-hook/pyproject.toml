[project]
name = "system-hook-example"
version = "0.1.0"
description = "Example project using pyrefly-typecheck-system hook"
requires-python = ">=3.9"
dependencies = [
    "packaging==25.0",
]

[project.optional-dependencies]
dev = [
    "pre-commit==4.5.0",
    "pyrefly==0.43.1",
]
